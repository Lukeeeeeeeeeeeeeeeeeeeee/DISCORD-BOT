const { execute } = require('../../services/recruiting/rookie-promote-service');

module.exports = {
  data: {
    name: 'rookie_promote',
    description: 'Verify and promote a rookie (MOD+ only)'
  },
  execute
};
